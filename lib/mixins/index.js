/**
 * Mixin functions
 * @module mixins
 */
'use strict'

const cacheMix = require('./cacheMix')
const specMix = require('./specMix')
const spotMix = require('./spotMix')
const subjectMix = require('./subjectMix')

module.exports = {
  cacheMix,
  specMix,
  spotMix,
  subjectMix
}
