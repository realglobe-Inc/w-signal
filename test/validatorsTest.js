/**
 * Test for validators.
 * Runs with mocha.
 */
'use strict'

const validators = require('../lib/validators')
const { ok, equal } = require('assert')

describe('validators', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
