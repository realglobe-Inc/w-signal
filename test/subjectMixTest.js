/**
 * Test for subjectMix.
 * Runs with mocha.
 */
'use strict'

const subjectMix = require('../lib/mixins/subjectMix')
const { ok, equal } = require('assert')

describe('subject-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
