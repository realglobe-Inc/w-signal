/**
 * Test for specMix.
 * Runs with mocha.
 */
'use strict'

const specMix = require('../lib/mixins/specMix')
const { ok, equal } = require('assert')

describe('spec-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
