/**
 * Test for cacheMix.
 * Runs with mocha.
 */
'use strict'

const cacheMix = require('../lib/mixins/cacheMix')
const { ok, equal } = require('assert')

describe('cache-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
